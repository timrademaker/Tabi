#pragma once

namespace tabi
{
    /** The maximum number of entities that can exist at a time */
    inline constexpr size_t MAX_ENTITIES = 1024;

    /** The maximum number of components an entity can hold */
    inline constexpr size_t MAX_COMPONENTS = 32;
}